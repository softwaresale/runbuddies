import { AbstractEntityService } from './abstract-entity-service';

describe('AbstractEntityService', () => {
  it('should create an instance', () => {
    expect(new AbstractEntityService()).toBeTruthy();
  });
});
